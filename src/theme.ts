import { DefaultTheme } from 'styled-components';

const defaultTheme: DefaultTheme = {
  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },

  fonts: {
      main: "Rubik",
      sec: "Poppins"
  }
};

export { defaultTheme };